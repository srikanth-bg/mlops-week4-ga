apiVersion: apps/v1
kind: Deployment
metadata:
  name: wk7-log-ml-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: wk7-log-ml-service
  template:
    metadata:
      labels:
        app: wk7-log-ml-service
    spec:
      serviceAccountName: telemetry-access  # See below
      containers:
      - name: fastapi-container
        image: REPLACE_WITH_IMAGE                # CI will substitute this
        ports:
        - containerPort: 8200
        readinessProbe:
          httpGet:
            path: /ready_check
            port: 8200
          initialDelaySeconds: 5
          periodSeconds: 10
        livenessProbe:
          httpGet:
            path: /live_check
            port: 8200
          initialDelaySeconds: 15
          periodSeconds: 20
        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
          limits:
            cpu: "500m"
            memory: "512Mi"
